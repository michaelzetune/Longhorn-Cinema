@model LonghornCinemaFinalProject.Models.Order
@*@using LonghornCinemaFinalProject.Models*@

@{
    ViewBag.Title = "Create";
}

<h2>Seat Selection</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.OrderID)

        <div>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    Do you want to create an order for this showing?
                </dt>
            </dl>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create Order" class="btn btn-primary" />
            </div>
        </div>
    </div>
}
@*@Html.ActionLink("Create Order", "Create", "Tickets", new { OrderID = Model.OrderID, ShowingID = ViewBag.CurrentShowingID }, new { @class = "btn btn-primary" })*@

<div>
    @Html.ActionLink("Back to Showings", "Index", "Showings")
</div>
